[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "bev-perception-nuscenes"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["scripts*"]